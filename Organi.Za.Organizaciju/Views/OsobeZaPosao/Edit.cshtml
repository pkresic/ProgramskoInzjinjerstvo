@model Organi.Za.Organizaciju.Data.DodijeljeneOsobe

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>DodijeljeneOsobe</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <label asp-for="PosaoId" class="control-label">Odabrani posao (nije moguće mijenjati)</label>
            <input asp-for="PosaoId" hidden="hidden" value="@ViewData["PosaoId"]" />
            <span class="form-control disabled">@ViewData["NazivPosla"]</span>
            <span asp-validation-for="PosaoId" class="text-danger"></span>
            <div class="form-group">
                <label asp-for="OsobaId" class="control-label"></label>
                <select asp-for="OsobaId" class="form-control" asp-items="ViewBag.OsobaId"></select>
                <span asp-validation-for="OsobaId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BrojOsoba" hidden="hidden" class="control-label"></label>
                <input hidden="hidden" asp-for="BrojOsoba" class="form-control" />
                <span asp-validation-for="BrojOsoba" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-route-id="@Model.PosaoId" asp-action="Index">Nazad</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
